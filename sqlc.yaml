version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: "internal/queries"
    # emit_json_tags: true
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"